import React from 'react';
import ReactDOM from 'react-dom';

const App = (_props) => {
  return (
    <div>Test apollo invalidation app</div>
  )
};

ReactDOM.render(<App />, document.querySelector('#app'));
